name: Test Workflow

on:
  push:
    paths:
      - '**/*.md'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Say hello
        run: echo "GitHub Actions triggered successfully!"
